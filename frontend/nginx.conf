# O Angular é uma SPA: qualquer rota desconhecida devolve o index.html
# e o router do Angular trata do resto.
server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
